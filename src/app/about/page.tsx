import BaseLayout from "@/layouts/base-layout";

export const metadata = {
    title: 'О компании — fundament',
};

export default function AboutPage() {
    return <BaseLayout>
        <div>О нашей компании</div>
    </BaseLayout>;
}
